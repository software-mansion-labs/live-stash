# Managed by Ansible (roles/app/templates/app.env.j2) — do not edit on the VM.
SECRET_KEY_BASE={{ secret_key_base }}
PHX_SERVER=true
PHX_HOST={{ private_ip }}
PORT=4000

LIVE_STASH_REDIS_URL=redis://{{ redis_private_ip }}:6379

RELEASE_DISTRIBUTION=name
RELEASE_NODE=testing@{{ private_ip }}
RELEASE_COOKIE={{ release_cookie }}
CLUSTER_HOSTS={{ cluster_hosts }}
